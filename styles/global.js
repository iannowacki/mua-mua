import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
    bodyText: {
        
        fontSize: 24,
        color: '#FFFFFF',

    },
    newBookForm: {
        borderWidth: 2,
        borderColor: '#eee',
        padding: 15,
        fontSize: 24,
        borderRadius: 6,
    }
});